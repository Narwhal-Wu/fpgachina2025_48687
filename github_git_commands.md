# GitHub 操作手册（常用 Git 指令指南）

本手册简要介绍 Git 在 GitHub 项目开发中的常用操作，适合初学者快速上手。

---

## 1. 仓库初始化与克隆

- 初始化本地仓库：
```bash
git init
```

- 克隆远程仓库：
```bash
git clone <仓库地址>
```

---

## 2. 分支操作

- 查看所有本地分支：
```bash
git branch
```

- 查看所有本地和远程分支：
```bash
git branch -a
```

- 新建分支：
```bash
git branch <分支名>
```

- 切换分支：
```bash
git checkout <分支名>
```

- 新建并切换分支：
```bash
git checkout -b <分支名>
```

- 删除本地分支：
```bash
git branch -d <分支名>
```

- 删除远程分支：
```bash
git push origin --delete <分支名>
```

---

## 3. 文件操作

- 查看工作区状态：
```bash
git status
```

- 添加文件到暂存区：
```bash
git add <文件名>
```

- 添加所有修改：
```bash
git add .
```

- 删除文件：
```bash
git rm <文件名>
```

- 移动/重命名文件：
```bash
git mv <旧文件名> <新文件名>
```

- 下载远程更新到本地（不合并）：
```bash
git fetch
```

- 拉取远程更新并合并：
```bash
git pull
```

---

## 4. 提交操作

- 提交文件（需先 `git add`）：
```bash
git commit -m "提交说明"
```

- 修改最后一次提交信息：
```bash
git commit --amend -m "新的提交说明"
```

- 推送到远程仓库：
```bash
git push origin <分支名>
```

---

## 5. 历史与日志

- 查看提交历史：
```bash
git log
```

- 查看简洁历史：
```bash
git log --oneline
```

- 查看带分支图的历史：
```bash
git log --oneline --graph --all
```

- 查看某个文件的历史：
```bash
git log <文件名>
```

- 查看某个提交详情：
```bash
git show <提交ID>
```

---

## 6. 撤销与回退

- 撤销工作区修改：
```bash
git checkout -- <文件名>
```

- 取消暂存区的修改：
```bash
git reset HEAD <文件名>
```

- 回退到某次提交（保留修改）：
```bash
git reset --soft <提交ID>
```

- 回退到某次提交（丢弃修改）：
```bash
git reset --hard <提交ID>
```

---

## 7. 远程仓库

- 查看远程仓库：
```bash
git remote -v
```

- 添加远程仓库：
```bash
git remote add origin <仓库地址>
```

- 修改远程仓库地址：
```bash
git remote set-url origin <新地址>
```

---

## 8. 标签操作

- 新建标签：
```bash
git tag <标签名>
```

- 查看标签：
```bash
git tag
```

- 推送标签：
```bash
git push origin <标签名>
```

- 删除标签：
```bash
git tag -d <标签名>
```

- 删除远程标签：
```bash
git push origin :refs/tags/<标签名>
```

---

## 9. 常用图形化查看

- 查看分支结构：
```bash
git log --oneline --graph --decorate --all
```

- 查看当前分支与远程的差异：
```bash
git diff origin/<分支名>
```

---

## 🔑 小结
以上是 GitHub 项目开发中最常用的 Git 指令。掌握这些，就可以完成从创建、分支管理、提交推送、版本回退到查看历史的大部分工作。

